============
Installation
============

At the command line::

    $ easy_install django-ltree-field

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-ltree-field
    $ pip install django-ltree-field
